<h1 class="title">
    CADASTRO
</h1>

<%= form_for @user do |f| %>
    <%= render 'form', f: f %>
    <%= f.number_field :voted, value: 0, type: 'hidden' %>
    <%= f.submit 'Cadastrar', class: 'btn btn-primary' %>
<% end %>